[package]
name = "wasm-std-demo"
version = "0.0.0"
authors = ["NikVolf <nikvolf@gmail.com>"]
target-dir = "wasm"

[dependencies]
pwasm-std = { path = "../" }

[lib]
path = "src/demo.rs"
crate-type = ["cdylib"]

[profile.release]
panic = "abort"
lto = true
